import reactCSS from 'reactcss'

export default reactCSS({
  default: {
    container: {
      backgroundColor: '#4CAF50',
      height: '100%',
      width: '100%',
      display: 'flex',
      flexDirection: 'column',
    }
  },
})
